# Cloudflare Workers 环境变量配置示例
# 复制此文件为 .dev.vars 用于本地开发

# JWT密钥（必需）- 用于生成和验证用户Token
# 生产环境请使用强随机字符串：openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here_change_in_production

# 管理员密钥（必需）- 用于管理后台认证
# 生产环境请使用强随机字符串：openssl rand -base64 32
ADMIN_SECRET_KEY=your_admin_secret_here_change_in_production

# TMDB API密钥（可选）- 用于获取电影评分和元数据
TMDB_API_KEY=your_tmdb_api_key_here

# 豆瓣API密钥（可选）- 用于获取豆瓣评分
DOUBAN_API_KEY=your_douban_api_key_here

# 钉钉Webhook（可选）- 用于系统告警通知
DINGTALK_WEBHOOK=

# 允许的CORS来源（可选）- 逗号分隔的域名列表
# 示例：https://admin.example.com,https://www.example.com
ALLOWED_ORIGINS=

# API安全配置（可选）
# API_SECRET_KEY=your_api_secret_key_here
# APP_PACKAGES=com.example.app1,com.example.app2
